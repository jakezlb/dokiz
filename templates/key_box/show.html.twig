{% extends 'base.html.twig' %}

{% block title %}KeyBox{% endblock %}

{% block body %}
    <h1>KeyBox</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ key_box.id }}</td>
            </tr>
            <tr>
                <th>Adress_key_box</th>
                <td>{{ key_box.adressKeyBox }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('key_box_index') }}">back to list</a>

    <a href="{{ path('key_box_edit', {'id': key_box.id}) }}">edit</a>

    {{ include('key_box/_delete_form.html.twig') }}
{% endblock %}
