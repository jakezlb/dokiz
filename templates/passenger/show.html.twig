{% extends 'base.html.twig' %}

{% block title %}Passenger{% endblock %}

{% block body %}
    <h1>Passenger</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ passenger.id }}</td>
            </tr>
            <tr>
                <th>Is_driver</th>
                <td>{{ passenger.isDriver ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('passenger_index') }}">Retour</a>

    <a href="{{ path('passenger_edit', {'id': passenger.id}) }}">edit</a>

    {{ include('passenger/_delete_form.html.twig') }}
{% endblock %}
