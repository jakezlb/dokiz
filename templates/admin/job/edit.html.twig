{% extends 'admin/base.html.twig' %}

{% block title %}Modifier le métier {{ job.wording }}{% endblock %}

{% block body %}
    {{ include("admin/header.html.twig") }}  
    {{ include("navigation/navAdmin.html.twig") }}
      
        <div class="row">
            <div class="col-sm-6">               
                <h1>Modifier le métier {{ job.wording }}</h1>
            </div>
            <div class="col-sm-6">
                <a class="btn btn-primary pull-right" href="{{ path('admin_job_index') }}" style="margin-left: 10%;">Retour</a>
                {{ include('admin/job/_delete_form.html.twig') }}
            </div>
        </div>

    {{ include('admin/job/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    {{ include("admin/footer.html.twig") }} 
{% endblock %}
