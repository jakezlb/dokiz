{% extends 'admin/base.html.twig' %}

{% block title %}New Job{% endblock %}

{% block body %}
    {{ include("admin/header.html.twig") }}
    {{ include("navigation/navAdmin.html.twig") }}
       <div class="row">
            <div class="col-sm-6">               
                <h1>Nouveau</h1>
            </div>
            <div class="col-sm-6">
                <a class="btn btn-primary pull-right" href="{{ path('admin_job_index') }}">Retour</a>
            </div>
        </div>
        {{ include('admin/job/_form.html.twig', {'button_label': 'Enregistrer'}) }}    
    {{ include("admin/footer.html.twig") }} 
{% endblock %}
