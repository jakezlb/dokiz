{% extends 'admin/base.html.twig' %}

{% set car = key_car.car %}

{% block title %}Modifier la clé de la {{ car.name }}{% endblock %}

{% block body %}
    {{ include("admin/header.html.twig") }} 
    {{ include("navigation/navAdmin.html.twig") }}
        <div class="row">
            <div class="col-sm-6">

                <h1>Modifier la clé de la {{ car.name }}</h1>
            </div>
            <div class="col-sm-6">
                <a class="btn btn-primary pull-right" href="{{ path('admin_key_car_index') }}" style="margin-left: 10%;">Retour</a>
                {{ include('admin/key_car/_delete_form.html.twig') }}
            </div>
        </div>

    {{ include('admin/key_car/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    {{ include("admin/footer.html.twig") }} 
{% endblock %}
