<form method="post" action="{{ path('admin_society_delete', {'id': society.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ society.id) }}">
    <button class="btn btn-danger pull-right">Supprimer</button>
</form>
