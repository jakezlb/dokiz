{% extends 'admin/base.html.twig' %}

{% block title %}Modifier la voiture{% endblock %}

{% block body %}
    {{ include("admin/header.html.twig") }}
    {{ include("navigation/navAdmin.html.twig") }}
    
        <div class="row">
            <div class="col-sm-6">
                <h1>Modifier la voiture {{ car.name }}</h1>
            </div>
            <div class="col-sm-6">
                <a class="btn btn-primary pull-right" href="{{ path('admin_car_index') }}" style="margin-left: 10%;">Retour</a>
                {{ include('admin/car/_delete_form.html.twig') }}
            </div>
        </div>
        {{ include('admin/car/_form.html.twig', {'button_label': 'Enregistrer'}) }}

        {{ include("admin/footer.html.twig") }}
{% endblock %}
