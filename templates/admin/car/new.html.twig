{% extends 'admin/base.html.twig' %}

{% block title %}Nouvelle voiture{% endblock %}

{% block body %}
    {{ include("admin/header.html.twig") }}
    {{ include("navigation/navAdmin.html.twig") }}

    <div class="row">
        <div class="col-sm-6">
            <h1>Ajouter une voiture</h1>
        </div>
        <div class="col-sm-6">
            <a class="btn btn-primary pull-right" href="{{ path('admin_car_index') }}">Retour</a>
        </div>
    </div>

    {{ include('admin/car/_form.html.twig') }}
    {{ include("admin/footer.html.twig") }}
{% endblock %}
