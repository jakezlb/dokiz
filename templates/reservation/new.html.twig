{% extends 'base.html.twig' %}

{% block javascripts %}
    <script>
        let lon1 = 0;
        let lat1 = 0;
        let lon2 = 0;
        let lat2 = 0;

        function initMap() {
            var mymap = L.map('mapid').setView([47.2305, -1.33], 10);

            L.tileLayer('https://api.mapbox.com/styles/v1/{id}/tiles/{z}/{x}/{y}?access_token={accessToken}', {
                attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors, <a href="https://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, Imagery © <a href="https://www.mapbox.com/">Mapbox</a>',
                maxZoom: 18,
                id: 'mapbox/streets-v11',
                tileSize: 512,
                zoomOffset: -1,
                accessToken: 'pk.eyJ1IjoiZ2VvZmZyYXkiLCJhIjoiY2thZTBrMDVtMDl0ZzJ5bnZxZjk3eXNoYiJ9.2cdRl4dFI2m39Yv8mdhfNA'
            }).addTo(mymap);

            return mymap;
        }

        // function addPoint(lon1, lat1, lon2, lat2) {
        //     mymap = initMap();
        //     if (lon1 !== 0 && lat1 !== 0) {
        //         var marker1 = L.marker([lat1, lon1]).addTo(mymap);
        //         if (lon2 !== 0 && lat2 !== 0) {
        //             var marker2 = L.marker([lat2, lon2]).addTo(mymap);
        //         }
        //     }
        // }

        $("#reservation_carRides_0_postal_code_point_departure").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][0][postal_code_point_departure]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        var postcodes = [];
                        response($.map(data.features, function (item) {
                            // Ici on est obligé d'ajouter les CP dans un array pour ne pas avoir plusieurs fois le même
                            if ($.inArray(item.properties.postcode, postcodes) == -1) {
                                postcodes.push(item.properties.postcode);
                                return { label: item.properties.postcode + " - " + item.properties.city,
                                    city: item.properties.city,
                                    value: item.properties.postcode
                                };
                            }
                        }));
                    }
                });
            },
            // On remplit aussi la ville
            select: function(event, ui) {
                $('#reservation_carRides_0_city_point_departure').val(ui.item.city);
            }
        });
        $("#reservation_carRides_1_postal_code_point_departure").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][1][postal_code_point_departure]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        var postcodes = [];
                        response($.map(data.features, function (item) {
                            // Ici on est obligé d'ajouter les CP dans un array pour ne pas avoir plusieurs fois le même
                            if ($.inArray(item.properties.postcode, postcodes) == -1) {
                                postcodes.push(item.properties.postcode);
                                return { label: item.properties.postcode + " - " + item.properties.city,
                                    city: item.properties.city,
                                    value: item.properties.postcode
                                };
                            }
                        }));
                    }
                });
            },
            // On remplit aussi la ville
            select: function(event, ui) {
                $('#reservation_carRides_1_city_point_departure').val(ui.item.city);
            }
        });

        $("#reservation_carRides_0_adress_point_departure").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][0][postal_code_point_departure]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        response($.map(data.features, function (item) {
                            // addPoint(item.geometry.coordinates[0], item.geometry.coordinates[1], 0, 0);
                            return {
                                label: item.properties.name,
                                value: item.properties.name,
                            };
                        }));
                    }
                });
            }
        });
        $("#reservation_carRides_1_adress_point_departure").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][1][postal_code_point_departure]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        response($.map(data.features, function (item) {
                            // addPoint(item.geometry.coordinates[0], item.geometry.coordinates[1], 0, 0);
                            return {
                                label: item.properties.name,
                                value: item.properties.name,
                            };
                        }));
                    }
                });
            }
        });

        $("#reservation_carRides_0_postal_code_point_arrival").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][0][postal_code_point_arrival]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        var postcodes = [];
                        response($.map(data.features, function (item) {
                            // Ici on est obligé d'ajouter les CP dans un array pour ne pas avoir plusieurs fois le même
                            if ($.inArray(item.properties.postcode, postcodes) == -1) {
                                postcodes.push(item.properties.postcode);
                                return { label: item.properties.postcode + " - " + item.properties.city,
                                    city: item.properties.city,
                                    value: item.properties.postcode
                                };
                            }
                        }));
                    }
                });
            },
            // On remplit aussi la ville
            select: function(event, ui) {
                $('#reservation_carRides_0_city_point_arrival').val(ui.item.city);
            }
        });
        $("#reservation_carRides_1_postal_code_point_arrival").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][1][postal_code_point_arrival]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        var postcodes = [];
                        response($.map(data.features, function (item) {
                            // Ici on est obligé d'ajouter les CP dans un array pour ne pas avoir plusieurs fois le même
                            if ($.inArray(item.properties.postcode, postcodes) == -1) {
                                postcodes.push(item.properties.postcode);
                                return { label: item.properties.postcode + " - " + item.properties.city,
                                    city: item.properties.city,
                                    value: item.properties.postcode
                                };
                            }
                        }));
                    }
                });
            },
            // On remplit aussi la ville
            select: function(event, ui) {
                $('#reservation_carRides_1_city_point_arrival').val(ui.item.city);
            }
        });

        $("#reservation_carRides_0_adress_point_arrival").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][0][postal_code_point_arrival]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        response($.map(data.features, function (item) {
                            return { label: item.properties.name, value: item.properties.name};
                        }));
                    }
                });
            }
        });
        $("#reservation_carRides_1_adress_point_arrival").autocomplete({
            source: function (request, response) {
                $.ajax({
                    url: "https://api-adresse.data.gouv.fr/search/?postcode="+$("input[name='reservation[carRides][1][postal_code_point_arrival]']").val(),
                    data: { q: request.term },
                    dataType: "json",
                    success: function (data) {
                        response($.map(data.features, function (item) {
                            return { label: item.properties.name, value: item.properties.name};
                        }));
                    }
                });
            }
        });

        function getDistanceFromLatLonInKm(lat1,lon1,lat2,lon2) {
            var R = 6371; // Radius of the earth in km
            var dLat = deg2rad(lat2-lat1);  // deg2rad below
            var dLon = deg2rad(lon2-lon1);
            var a =
                Math.sin(dLat/2) * Math.sin(dLat/2) +
                Math.cos(deg2rad(lat1)) * Math.cos(deg2rad(lat2)) *
                Math.sin(dLon/2) * Math.sin(dLon/2)
            ;
            var c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
            var d = R * c; // Distance in km
            return d;
        }

        function deg2rad(deg) {
            return deg * (Math.PI/180)
        }

        // $("#car_ride_adress_point_departure").blur(function() {
        //     if($(this).val() != '') {
        //         $('#mapid').css('display', 'block');
        //     } else {
        //         $('#mapid').css('display', 'none');
        //     }
        // });
    </script>
{% endblock %}

{% block title %}Nouvelle réservation{% endblock %}

{% block body %}
<header class="masthead backWhite" id="dokiz">
    <div class="container align-items-center boxShadow p-3 mb-5 bg-white rounded jumbotron">
    {{ include("navigation/navReservation.html.twig") }}
        <div class="row mx-auto" style="margin-top: 40px" >
            <h1 class="titleReservation ">Nouvelle réservation</h1>
        </div>
        <hr>
        <div class="row">
            <div class="col-12" style="height: 15em; display: none;" id="mapid"></div>
        </div>
        <div class="row">
            <div class="col-12">
                {{ include('reservation/_form.html.twig') }}
            </div>
        </div>
    <a href="{{ path('reservation_index') }}">Retour à la liste des réservations</a>
    </div>
</header>
{% endblock %}
