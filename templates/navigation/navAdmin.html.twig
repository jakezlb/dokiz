<nav class="navbar navbar-expand-lg" id="adminNav">
    <a  class="nav-link" href="{{path('index') }}"><img src="{{ asset('image/Blanc.png') }}" title="source: imgur.com" class="img-fluid logo" alt=""></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">       
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Utilisateurs
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{{path('status_index') }}">Gestion des droits</a>         
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Float de véhicules
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{{path('car_index') }}">Liste des véhicules</a> 
          <a class="dropdown-item" href="{{path('car_new') }}">>Ajouter un véhicule</a>            
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Gestion des trajets et des réservations
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{{path('car_ride_index') }}">Liste des trajets</a> 
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path('reservation_index') }}">Liste des réservations</a>            
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Eléments
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{{path('job_index') }}">Liste des postes</a> 
          <a class="dropdown-item" href="{{path('job_new') }}">Ajouter un poste</a>            
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path('society_index') }}">Liste des entreprises</a> 
          <a class="dropdown-item" href="{{path('society_new') }}">>Ajouter une entreprises</a> 
        </div>
      </li>
      <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
    </ul>
  </div>
</nav>